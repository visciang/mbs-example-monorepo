name: CI

on: ["push", "pull_request"]

jobs:
  ci:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: |
          ./mbs.sh build run --verbose --sandbox

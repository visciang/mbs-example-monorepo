{
    "id": "docker_app",
    "component": {
        "toolchain": "toolchain-deploy-docker_compose",
        "toolchain_opts": ["--load-only"],
        "build_target_dependencies": [
            "docker://docker_app"
        ]
    }
}